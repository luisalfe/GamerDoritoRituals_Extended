<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Preferencia para seleccionar el tamaño del texto -->
    <ListPreference
        android:key="text_size"
        android:title="Tamaño del texto"
        android:summary="Selecciona el tamaño del texto en la pantalla de aceleración"
        android:entries="@array/text_size_options"
        android:entryValues="@array/text_size_values"
        android:defaultValue="16" />

    <!-- Preferencia para seleccionar el color del texto -->
    <ListPreference
        android:key="text_color"
        android:title="Color del texto"
        android:summary="Selecciona el color del texto en la pantalla de aceleración"
        android:entries="@array/text_color_options"
        android:entryValues="@array/text_color_values"
        android:defaultValue="#000000" />

</PreferenceScreen>
